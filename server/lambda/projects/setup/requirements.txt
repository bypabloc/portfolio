# Lambda-specific dependencies for projects service
# (Shared dependencies are in server/shared/requirements.txt)

# No additional dependencies required for this Lambda
# All core dependencies (FastAPI, SQLModel, AsyncPG) come from shared/
