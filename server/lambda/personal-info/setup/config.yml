# Portfolio Serverless System - Personal Info Lambda Configuration
# Unified configuration file for Lambda function and API Gateway

lambda_function:
  name: personal-info
  description: "Personal information management Lambda function"
  language: python
  runtime: python3.13
  handler: src.lambda_function.lambda_handler
  package_file: requirements.txt
  package_type: zip
  service_name: personal-info
  timeout: 30
  memory_size: 512


  # Shared modules this lambda uses
  shared_modules:
    - database
    - models
    - repository
    - config
    - utils
    - exceptions

api_gateway:
  path: "/personal-info"
  cors_enabled: true
  methods:
    - GET
    - POST
    - PUT
    - DELETE

  # CORS configuration
  cors:
    allow_origins:
      - "*"
    allow_methods:
      - "GET"
      - "POST"
      - "PUT"
      - "DELETE"
      - "OPTIONS"
    allow_headers:
      - "Content-Type"
      - "X-Amz-Date"
      - "Authorization"
      - "X-Api-Key"
      - "X-Amz-Security-Token"
      - "X-Correlation-ID"
    max_age: 3600

# Shared modules deployment configuration
deployment:
  strategy: copy
  target_dir: shared
  preserve_structure: true
  include_compiled: false
  exclude_patterns:
    - "*.md"
    - "*.txt"
    - "test_*"
    - "*_test.py"

# Environment variables configuration
environment:
  required:
    - DATABASE_URL
    - ENVIRONMENT
  optional:
    - LOG_LEVEL
    - DEBUG_MODE

# Health check configuration
health_check:
  enabled: true
  endpoint: /health
  dependencies:
    - database
    - shared_modules

# Monitoring configuration
monitoring:
  cloudwatch_logs: true